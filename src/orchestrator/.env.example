# Domain where the service is running (with protocol)
DOMAIN=https://example.com

# Application root path (for subdirectory deployment)
# Use / for root deployment
# For Nginx Proxy Manager with Custom Location, set this to match your Custom Location path (e.g., /admin)
APPLICATION_ROOT=/admin

# Orchestrator password
PASSWORD_ORCHESTRATOR=your_secure_password

# Secret key for session encryption
SECRET_KEY=your_secret_key
